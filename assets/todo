Jobs to do

fix progress bar update problem
fix date calculation math?
set time done / left to change on press


make bg loading happen on its own thread.... done?

light and dark theme.

There are two DB in the DB adapter, change to only use one

change bitmap factory workings minor problems with scaling...
